# .env.common - Common secrets shared across ALL your apps
# 
# This file is for secrets that you want available in every application
# you develop. The Tiltfile will automatically create a 'common-secret'
# from these values.
#
# IMPORTANT: 
# - Copy this to .env.common and add your actual values
# - .env.common is gitignored - never commit it!
# - Use simple KEY=VALUE format (no SECRET_ prefix needed)

# Example: AWS Credentials
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_DEFAULT_REGION=us-west-2

# Example: Database connection (shared dev database)
DATABASE_URL=postgresql://user:password@localhost:5432/devdb

# Example: API Keys for external services
STRIPE_API_KEY=sk_test_your_stripe_key
SENDGRID_API_KEY=your_sendgrid_key

# Example: Redis connection
REDIS_URL=redis://localhost:6379

# Example: S3 bucket
S3_BUCKET=my-dev-bucket

# Add any other secrets you want available across all apps
